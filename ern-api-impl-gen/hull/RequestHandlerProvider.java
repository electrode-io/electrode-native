package com.ern.api.impl;

public interface RequestHandlerProvider {
    /**
     * Method that holds the implementation to invoke all the request handler registrations for a given api.
     */
    void init();
}
