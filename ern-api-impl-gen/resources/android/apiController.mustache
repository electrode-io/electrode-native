{{>licenseInfo}}

package com.ern.api.impl;

import android.support.annotation.Nullable;
import android.util.Log;

{{>generatedCode}}

public final class {{apiName}}ApiController {
    private static {{apiName}}ApiRequestHandlerProvider sRequestHandler;

    private {{apiName}}ApiController() {
    }

    public static void register(
            @Nullable {{apiName}}ApiRequestHandlerProvider.{{apiName}}ApiConfig requestHandlerConfig) {
        if (sRequestHandler == null) {
            sRequestHandler = new {{apiName}}ApiRequestHandlerProvider(requestHandlerConfig);
            {{#requests}}
            sRequestHandler.register{{.}}RequestHandler();
            {{/requests}}
        } else {
            Log.w("{{apiName}}Controller", "Already registered, ignoring duplicate register call");
        }
    }
}
